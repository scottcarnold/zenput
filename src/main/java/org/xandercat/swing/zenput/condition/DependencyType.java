package org.xandercat.swing.zenput.condition;

/**
 * DependencyType represents whether the dependent condition should be a determination
 * into whether or not the field is validated or if it should be a validation in and of itself.
 * 
 * @author Scott Arnold
 */
public enum DependencyType {
	CONDITION, VALIDATION;
}
